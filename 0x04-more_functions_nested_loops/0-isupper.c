#include <main.h>

/**
 * _isupper - Check if a character is upper
 * @c: the number to be checked
 * Return : 1 for upper ltter or o for anything else
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 98)
	{
	return (1);
	}
	return (0);
}
