#include <stdio.h>

/**
 *  * main - Default function
 *   *
 *    * Return: Returns a 1 (Success)
 *     */
int main(void)
{
		char letter;

			for (letter = 'a'; letter <= 'z'; letter++)
						putchar(letter);

				putchar('\n');

					return (0);
}
